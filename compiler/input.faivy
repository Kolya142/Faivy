extern write :: libc_write :: usize (fd: i32, ptr: usize, size: usize);
extern strlen :: libc_strlen :: usize (str: usize);

global :: main :: i32 () {
    1 :: if {
        libc_write(cast(1, i32), "Hello, world!", libc_strlen("Hello, world!"));
    }
    0 :: if {
        return(42);
    }
    else {
        return(69+42);
    }
}
